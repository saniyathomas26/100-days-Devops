
# Day13
## Task
Secure Apache service running on port 8088 by restricting access using iptables so that only the LBR host can access it, and ensure rules persist after reboot.
## Purpose 
To prevent unauthorized network access to Apache by enforcing host-level firewall rules while allowing legitimate traffic from the load balancer.
